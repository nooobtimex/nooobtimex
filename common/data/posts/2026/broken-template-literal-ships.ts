import type { PostDef } from '../../../interfaces'

/**
 * STUB — a reserved slot in the journey. Promote by filling the AEO fields and removing `draft`.
 * Anchor: memory/lessons/html-deck-verification.md — Thai text renders ~20% taller
 */
export const brokenTemplateLiteralShips: PostDef = {
	id: 'broken-template-literal-ships',
	title: 'A broken template literal does not throw — it ships',
	happenedAt: '2026-08-22',
	chapter: 'ownership',
	draft: true
}
