import type { PostDef } from '../../../interfaces'

/**
 * STUB — a reserved slot in the journey. Promote by filling the AEO fields and removing `draft`.
 * Anchor: same — the trailing-60-day price ranking signal + the written price-expiry rule
 */
export const cheapMakesYouInvisible: PostDef = {
	id: 'cheap-makes-you-invisible',
	title: 'On this platform, cheap makes you invisible',
	happenedAt: '2026-08-22',
	chapter: 'freelance',
	draft: true
}
