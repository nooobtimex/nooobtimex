import type { PostDef } from '../../../interfaces'

/** STUB — a reserved slot in the journey. Promote by filling the AEO fields and removing `draft`. */
export const minioToR2StreamingProxyPii: PostDef = {
	id: 'minio-to-r2-streaming-proxy-pii',
	title: 'MinIO to Cloudflare R2 behind a provider-agnostic S3 layer, with a private PII bucket',
	happenedAt: '2026-06-04',
	chapter: 'scale',
	draft: true
}
