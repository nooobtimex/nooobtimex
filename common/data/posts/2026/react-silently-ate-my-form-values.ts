import type { PostDef } from '../../../interfaces'

/**
 * STUB — a reserved slot in the journey. Promote by filling the AEO fields and removing `draft`.
 * Anchor: memory/areas/fastwork-freelance.md — write as a React state lesson, not an automation recipe
 */
export const reactSilentlyAteMyFormValues: PostDef = {
	id: 'react-silently-ate-my-form-values',
	title: 'React silently ate my form values',
	happenedAt: '2026-08-24',
	chapter: 'freelance',
	draft: true
}
