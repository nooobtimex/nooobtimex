import type { PostDef } from '../../../interfaces'

/**
 * STUB — a reserved slot in the journey. Promote by filling the AEO fields and removing `draft`.
 * Anchor: index.csv + Design-Assets — a rebrand visible as data
 */
export const tealEraNavyEra: PostDef = {
	id: 'teal-era-navy-era',
	title: 'The teal era and the navy era, sitting in the same folder',
	happenedAt: '2026-08-22',
	chapter: 'ownership',
	draft: true
}
